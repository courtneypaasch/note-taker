//establish initial requires and port 
const fs = require("fs");
const express = require("express");
const path = require("path");
const api = require(".db/db.json");
const app = express();
const port = 3000;

